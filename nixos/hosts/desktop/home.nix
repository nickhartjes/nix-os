##################
## Hardware specific applications
##################

{ pkgs, ... }:

{
  imports =
    [
    ];

  home = {
    packages = with pkgs; [
    ];
  };

  programs = {
  };

  services = {
  };
}
